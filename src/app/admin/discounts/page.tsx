'use client'

import { useState, type FormEvent } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { toast } from 'sonner'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Checkbox } from '@/components/ui/checkbox'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from '@/components/ui/alert-dialog'
import { createDiscount, deleteDiscount, getDiscounts, updateDiscount, type Discount, type DiscountInput } from '@/api/discount'
import { useI18n } from '@/lib/i18n'

const initial: DiscountInput = { name: '', amount: 0, type: 'percent', note: '', active: true }

export default function DiscountsPage() {
  const client = useQueryClient(); const { t, locale } = useI18n(); const [form, setForm] = useState<DiscountInput>(initial); const [editing, setEditing] = useState<Discount | null>(null)
  const { data: discounts = [], isLoading } = useQuery({ queryKey: ['discounts'], queryFn: getDiscounts })
  const save = useMutation({ mutationFn: () => editing ? updateDiscount({ id: editing._id, data: form }) : createDiscount(form), onSuccess: () => { client.invalidateQueries({ queryKey: ['discounts'] }); toast.success(editing ? t('updateDiscountSuccess') : t('createDiscountSuccess')); setForm(initial); setEditing(null) }, onError: () => toast.error(t('discountSaveError')) })
  const remove = useMutation({ mutationFn: deleteDiscount, onSuccess: () => { client.invalidateQueries({ queryKey: ['discounts'] }); toast.success(t('deleteDiscountSuccess')) }, onError: () => toast.error(t('discountDeleteError')) })

  function submit(event: FormEvent) { event.preventDefault(); if (!form.name.trim()) return toast.error(t('requiredDiscountName')); save.mutate() }
  function startEditing(discount: Discount) { setEditing(discount); setForm({ name: discount.name, amount: discount.amount, type: discount.type, note: discount.note, active: discount.active }) }
  function cancelEditing() { setForm(initial); setEditing(null) }
  function formatAmount(discount: Discount) { return discount.type === 'percent' ? `${discount.amount}%` : `${discount.amount.toLocaleString(locale)} ${t('currency')}`.trim() }

  return <div className="p-6 md:p-8"><div className="mb-6"><h1 className="text-3xl font-bold">{t('discounts')}</h1></div><div className="grid gap-6 lg:grid-cols-[340px_1fr]"><Card className="h-fit"><CardHeader><CardTitle>{editing ? t('editDiscount') : t('createDiscount')}</CardTitle></CardHeader><CardContent><form className="space-y-4" onSubmit={submit}><div className="space-y-2"><Label htmlFor="discount-name">{t('name')}</Label><Input id="discount-name" placeholder={t('discountNamePlaceholder')} value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></div><div className="grid grid-cols-2 gap-2"><div className="space-y-2"><Label htmlFor="discount-type">{t('discountType')}</Label><select id="discount-type" className="h-9 w-full rounded-md border bg-background px-3 text-sm" value={form.type} onChange={(e) => setForm({ ...form, type: e.target.value as DiscountInput['type'] })}><option value="percent">{t('discountPercent')}</option><option value="value">{t('discountValue')}</option></select></div><div className="space-y-2"><Label htmlFor="discount-amount">{t('discountAmount')}</Label><Input id="discount-amount" type="number" min="0" value={form.amount} onChange={(e) => setForm({ ...form, amount: Number(e.target.value) })} /></div></div><div className="space-y-2"><Label htmlFor="discount-note">{t('discountNote')}</Label><Input id="discount-note" placeholder={t('discountNotePlaceholder')} value={form.note} onChange={(e) => setForm({ ...form, note: e.target.value })} /></div><label className="flex items-center gap-2 text-sm"><Checkbox checked={form.active} onCheckedChange={(active) => setForm({ ...form, active: active === true })} />{t('discountActive')}</label><div className="flex gap-2"><Button type="submit" disabled={save.isPending}>{save.isPending ? t('savingDiscount') : t('save')}</Button>{editing && <Button type="button" variant="outline" onClick={cancelEditing}>{t('cancel')}</Button>}</div></form></CardContent></Card><Card><CardHeader><CardTitle>{t('discountList')}</CardTitle></CardHeader><CardContent><Table><TableHeader><TableRow><TableHead>{t('name')}</TableHead><TableHead>{t('discountAmount')}</TableHead><TableHead>{t('status')}</TableHead><TableHead className="text-right">{t('actions')}</TableHead></TableRow></TableHeader><TableBody>{isLoading ? <TableRow><TableCell colSpan={4}>{t('loading')}</TableCell></TableRow> : discounts.length === 0 ? <TableRow><TableCell colSpan={4} className="text-center text-muted-foreground">{t('emptyDiscounts')}</TableCell></TableRow> : discounts.map((discount) => <TableRow key={discount._id}><TableCell>{discount.name}</TableCell><TableCell>{formatAmount(discount)}</TableCell><TableCell>{discount.active ? t('discountActive') : t('hidden')}</TableCell><TableCell className="space-x-2 text-right"><Button size="sm" variant="outline" onClick={() => startEditing(discount)}>{t('edit')}</Button><AlertDialog><AlertDialogTrigger asChild><Button size="sm" variant="destructive" disabled={remove.isPending}>{remove.isPending ? t('deletingDiscount') : t('delete')}</Button></AlertDialogTrigger><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>{t('confirmDeleteTitle')}</AlertDialogTitle><AlertDialogDescription>{t('confirmDeleteDiscount')}</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>{t('cancel')}</AlertDialogCancel><AlertDialogAction variant="destructive" onClick={() => remove.mutate(discount._id)}>{t('confirm')}</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></TableCell></TableRow>)}</TableBody></Table></CardContent></Card></div></div>
}
