'use client'

import { useEffect, useMemo, useRef, useState } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { Loader2 } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Checkbox } from '@/components/ui/checkbox'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { getAddons, type Addon } from '@/api/addon'
import { addStoreAddon, getStoreAddons, updateStoreAddon } from '@/api/store-addon'
import { useI18n } from '@/lib/i18n'
import { useAuth } from '@/hooks/auth'
import { useStorePricingEmbedded } from '@/app/admin/store-pricing/store-pricing-context'

export default function StoreAddonsPanel() {
  const { locale, t } = useI18n(); const { user } = useAuth(); const embedded = useStorePricingEmbedded(); const client = useQueryClient(); const listRef = useRef<HTMLDivElement>(null)
  const canChangePermanent = user?.role === 'Admin' || user?.role === 'SuperAdmin'
  const [addonId, setAddonId] = useState(''); const [priceExtra, setPriceExtra] = useState(0); const [editingId, setEditingId] = useState<string | null>(null); const [draftPrice, setDraftPrice] = useState(0); const [draftPermanent, setDraftPermanent] = useState(true); const [draftTemporary, setDraftTemporary] = useState(false); const [page, setPage] = useState(1); const [pageSize, setPageSize] = useState(8)
  const { data: addons = [] } = useQuery({ queryKey: ['addons', locale], queryFn: () => getAddons(locale) }); const { data: storeAddons = [] } = useQuery({ queryKey: ['store-addons', locale], queryFn: () => getStoreAddons(locale) }); const refresh = () => client.invalidateQueries({ queryKey: ['store-addons'] }); const add = useMutation({ mutationFn: addStoreAddon, onSuccess: () => { refresh(); setAddonId(''); setPriceExtra(0) } }); const update = useMutation({ mutationFn: updateStoreAddon, onSuccess: () => { refresh(); setEditingId(null) } }); const available = addons.filter((addon) => !storeAddons.some((current) => current._id === addon._id)); const name = (addon: Addon) => addon.names[locale] || addon.names.vi || addon.name; const totalPages = Math.max(1, Math.ceil(storeAddons.length / pageSize)); const paginated = useMemo(() => storeAddons.slice((page - 1) * pageSize, page * pageSize), [storeAddons, page, pageSize])
  useEffect(() => { const element = listRef.current; if (!element) return; const resize = () => { const rows = Array.from(element.querySelectorAll<HTMLElement>('[data-store-row]')); const rowHeight = rows.length ? Math.max(...rows.map((row) => row.getBoundingClientRect().height)) : 74; setPageSize(Math.max(1, Math.floor((element.clientHeight + 12) / (rowHeight + 12)))) }; resize(); const observer = new ResizeObserver(resize); observer.observe(element); element.querySelectorAll<HTMLElement>('[data-store-row]').forEach((row) => observer.observe(row)); return () => observer.disconnect() }, [paginated.length, page, locale]); useEffect(() => setPage((current) => Math.min(current, totalPages)), [totalPages])
  return <div className={`flex h-full min-h-0 flex-col gap-6 overflow-hidden [&>[data-slot=card]]:border [&>[data-slot=card]]:border-border/50 [&>[data-slot=card]]:ring-0 ${embedded ? 'px-1 pb-6' : 'p-6 md:p-8'}`}>
    <Card className="shrink-0"><CardHeader><CardTitle>{t('createAddon')}</CardTitle></CardHeader><CardContent className="flex flex-wrap items-end gap-3"><div className="min-w-64 space-y-2"><Label>{t('addons')}</Label><Select value={addonId} onValueChange={setAddonId} disabled={!available.length}><SelectTrigger><SelectValue placeholder={available.length ? t('addons') : ''} /></SelectTrigger><SelectContent>{available.map((addon) => <SelectItem key={addon._id} value={addon._id}>{name(addon)}</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><Label>{t('extraPrice')}</Label><Input className="w-32" type="number" value={priceExtra} onChange={(event) => setPriceExtra(Number(event.target.value))} /></div><Button disabled={!addonId || add.isPending} onClick={() => add.mutate({ addonId, priceExtra, permanentlyActive: true })}>{add.isPending ? <Loader2 className="size-4 animate-spin" /> : t('createAddon')}</Button></CardContent></Card>
    <Card className="min-h-0 flex-1 flex flex-col"><CardHeader className="shrink-0"><div className="flex flex-wrap items-center justify-between gap-3"><CardTitle>{t('addonList')}</CardTitle><div className="flex items-center gap-3"><span className="text-sm text-muted-foreground">{t('total')}: {storeAddons.length}</span><div className="flex items-center gap-2"><Button size="sm" variant="outline" disabled={page === 1} onClick={() => setPage(page - 1)}>‹</Button><span className="text-sm text-muted-foreground">{page}/{totalPages}</span><Button size="sm" variant="outline" disabled={page === totalPages} onClick={() => setPage(page + 1)}>›</Button></div></div></div></CardHeader><CardContent ref={listRef} className="min-h-0 flex-1 overflow-hidden space-y-3">{paginated.map((addon) => { const isEditing = editingId === addon._id; const saving = isEditing && update.isPending; return <div data-store-row="true" className="grid min-h-[60px] gap-x-6 gap-y-3 rounded-lg border p-3 md:grid-cols-[minmax(0,1fr)_128px_minmax(110px,auto)_auto] md:items-center" key={addon._id}><div className="min-w-0 font-medium">{name(addon)}</div><div className="flex items-center"><div className="w-32">{isEditing ? <Input disabled={saving} className="h-9 w-full" type="number" value={draftPrice} onChange={(event) => setDraftPrice(Number(event.target.value))} /> : <div className="h-9 rounded-md border px-3 py-2 text-sm">{addon.priceExtra.toLocaleString()}</div>}</div></div><div className="flex flex-wrap items-center gap-2 text-sm">{isEditing && canChangePermanent && <label className="flex items-center gap-2"><Checkbox disabled={saving} checked={draftPermanent} onCheckedChange={(value) => { setDraftPermanent(value === true); if (value !== true) setDraftTemporary(false) }} />{t('permanentSelling')}</label>}{isEditing && draftPermanent && <label className="flex items-center gap-2"><Checkbox disabled={saving} checked={draftTemporary} onCheckedChange={(value) => setDraftTemporary(value === true)} />{t('temporaryUnavailable')}</label>}{!isEditing && <><span>{addon.permanentlyActive ? t('permanentSelling') : t('permanentHidden')}</span>{addon.permanentlyActive && <span>{addon.temporarilyUnavailable ? t('temporaryUnavailable') : t('temporaryAvailable')}</span>}</>}{saving && <Loader2 className="size-4 animate-spin text-primary" aria-label={t('loading')} />}</div><div className="flex min-h-9 items-center gap-2">{isEditing ? <><Button size="sm" disabled={saving} onClick={() => update.mutate({ addonId: addon._id, data: { priceExtra: draftPrice, ...(canChangePermanent ? { permanentlyActive: draftPermanent } : {}), temporarilyUnavailable: draftPermanent && draftTemporary } })}>{saving ? <Loader2 className="size-4 animate-spin" /> : t('save')}</Button><Button size="sm" variant="outline" disabled={saving} onClick={() => setEditingId(null)}>{t('cancel')}</Button></> : <Button size="sm" variant="outline" onClick={() => { setEditingId(addon._id); setDraftPrice(addon.priceExtra); setDraftPermanent(addon.permanentlyActive); setDraftTemporary(addon.permanentlyActive && addon.temporarilyUnavailable) }}>{t('edit')}</Button>}</div></div> })}</CardContent></Card>
  </div>
}
